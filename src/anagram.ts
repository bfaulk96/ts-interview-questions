/*
Write a function that checks if two provided strings are anagrams of each other; 
letter casing shouldn’t matter. Also, consider only characters, not spaces. 
For example:

anagram('finder', 'Friend') => true
anagram('hello', 'bye') => false
*/
export function isAnagram(str1: string, str2: string): boolean {
  return false;
}
